apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: default
spec:
  selector:
    apm.k8s.elastic.co/name: my-app
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: NodePort