apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: default
spec:
  selector:
    app: my-app
  ports:
    - name: http
      port: 3000
      nodePort: 30014
      targetPort: 3000
  type: NodePort